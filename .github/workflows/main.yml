name: GitHub Actions Demo
run-name: Testing github actions
on:
  push:
    branches: [ main ]
jobs:
  audit:
    name: npm audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        continue-on-error: true
      - name: install dependencies
        run: npm ci
      - name: audit
        run: npm audit
  build-docker:
    runs-on: ubuntu-latest
    steps:
      - name: test
      - run: echo "pushing to aws"